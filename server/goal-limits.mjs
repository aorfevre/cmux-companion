// The one length limit a goal's text is held to, wherever it is typed or
// generated. Data-only, so goal sessions and burst can share it without
// either importing the other.
export const MAX_GOAL_TEXT = 4_000;
